# custom_gistic2_plot
This repository has R scripts that can be used to prepare custom plots similar to AMp/Del Score GISTIC plot
# USAGE:
user need to provide the gistic output direcory as per their study for example (BM samples/ LUNG samples/ Normal etc). This can be done within script.
